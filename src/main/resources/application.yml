server:
  port: ${SERVICE_HOST}
  servlet:
    context-path: /profile

spring:
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}